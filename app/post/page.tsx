import Template3 from "@/components/post/ui-page/tamplate3";

// 게시글 목록을 보는 페이지 -> 작성일 순으로
const Page: React.FC = async () => {
  // return <Template5 />;
  return <Template3 />;
};

export default Page;
